---
- hosts: "{{ target_dbs }}"
  gather_facts: no
  become: no
  
  tasks:

     - block:

        - include_role:
            name: delius_audited_interaction
            tasks_from: call_audit_propagate_automation.yml

       delegate_to: localhost