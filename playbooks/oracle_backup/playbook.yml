---
- hosts: "{{ target }}"
  # connection: local

  tasks:
    - name: get hostname
      command: hostname
      register: hostname

    - name: debug
      ansible.builtin.debug:
        var: hostname
