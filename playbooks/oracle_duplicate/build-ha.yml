--- # build-ha.yml

- name: Build HA DB
  hosts: "{{ primary }} {{ standby }}"
  become: true
  become_user: root
  become_method: sudo
  roles:
    - role: hmpps-delius-oracledb-ha
      vars:
         force_restore: true