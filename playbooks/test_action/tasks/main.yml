- name: Test Action
  hosts: '{{ test_target }}'
  gather_facts: yes
  become: yes
  become_user: oracle
  become_method: sudo

  tasks:
    - name: Show Token
      shell: |
             echo ${GITHUB_TOKEN}