- name: Test Action
  hosts: '{{ test_target }}'
  gather_facts: yes
  become: yes
  become_user: oracle
  become_method: sudo

  tasks:
    - name: Show Token
      shell: |
             echo "1: {{ lookup('env','GITHUB_TOKEN') }}"
             echo "2: {{ github_token }}"